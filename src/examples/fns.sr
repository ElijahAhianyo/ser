//fn fib(n) {
//    if (n <= 1) { // TODO: taking out the brace fails. need to investigate why
//       return n
//    }
//    return fib(n - 2) + fib(n - 1);
//}

//for (var i = 0; i < 20; i = i + 1) {
//    print fib(i);
//}

print "===========================";

fn makeCounter() {
    var i = 0;
    fn count() {
        i = i + 1;
        print i;
    }
    return count;
  }
var counter = makeCounter();
counter(); // "1".
counter(); // "2".